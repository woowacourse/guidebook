# 랜딩 서사 아크 완성 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 랜딩(`content/index.mdx`)의 Hero와 CrewJourney 사이에 `TecoWay`(우테코 정체성+성장 3원칙, Manifesto 스타일) 섹션을 넣고, 별자리(`CrewVoiceMap`)를 행선지(`CrewJourney`) 앞으로 옮겨 "열망 → 정체성·방법 → 흔적 → 확산 → 초대" 서사를 완성한다.

**Architecture:** 기존 랜딩 섹션 컴포넌트(`CrewJourney`·`CrewVoiceMap`)와 동일한 패턴의 서버 컴포넌트를 하나 추가한다. 새 섹션은 기존 `Claim`(`components/Manifesto.tsx`)과 `Eyebrow`를 재사용하고, 규모 숫자는 `crewCount`(`content/crew-voices.ts`) 단일 원천을 공유해 별자리·행선지와 같은 실(735 throughline)로 꿴다. 순서 변경은 `content/index.mdx`의 `<Reveal>` 블록 재배열 + 상호참조 카피 2곳 정정으로 끝난다.

**Tech Stack:** Next.js 15 App Router, Nextra 4, React 19, CSS Modules, TypeScript 5.7. 테스트 프레임워크 없음(컴포넌트 단위 테스트 하네스 부재) — 검증은 dev 서버 컴파일 + 육안 + `next build` 타입 게이트.

## Global Constraints

- **카피 규칙:** 본문은 합니다체(~합니다/~습니다/~입니다). em-dash(`—`, U+2014) 금지 — 쉼표·마침표·괄호로 대체(이 repo `ai-style-lint` 규칙). tone/style 린터는 `content/education/*`만 스캔하므로 `TecoWay.tsx`·`index.mdx` 카피는 **손으로** 이 규칙을 지킨다.
- **단일 원천:** 크루 수는 항상 `crewCount`(`content/crew-voices.ts`)를 import해 쓴다. 735를 하드코딩하지 않는다.
- **커밋 격리:** 이 저장소는 Stop 훅이 매 턴 `git add -A`로 남은 변경을 쓸어 담는다. 병렬 WIP(`components/CrewGrowth.tsx`, `content/education/journey.mdx`, `content/education/_meta.ts`, `content/updates.ts` 등 형제 스펙 작업)가 있으므로, 각 커밋은 **명시한 정확한 파일 경로만** `git add`한다. `git add -A`/`git add .` 절대 금지.
- **범위 밖:** `CrewVoiceMap`·`CrewJourney` 내부 로직/비주얼은 건드리지 않는다(위치 이동 + 상호참조 카피만). `CrewGrowth`/`/education/journey`(형제 스펙)는 손대지 않는다. 신규 일러스트 없음.
- **컴포넌트 이름:** `TecoWay` (`CrewGrowth`는 형제 스펙이 선점).

---

### Task 1: TecoWay 섹션 생성 + 배선 (Hero 다음에 렌더)

Hero와 CrewJourney 사이에 새 선언 섹션을 만들어 랜딩에 띄운다. 이 태스크만으로 "새 섹션이 Hero 아래에서 형제 섹션과 같은 톤으로 읽히는가"를 독립적으로 리뷰할 수 있다. (순서 재배치는 Task 2.)

**Files:**
- Create: `components/TecoWay.tsx`
- Create: `components/TecoWay.module.css`
- Modify: `mdx-components.tsx` (import 추가 + return 객체에 등록)
- Modify: `components/index.ts` (export 추가)
- Modify: `content/index.mdx` (Hero 블록 바로 다음에 `<Reveal><TecoWay /></Reveal>` 삽입)

**Interfaces:**
- Consumes: `Eyebrow`(`{ children: ReactNode }`), `Claim`(`{ n?: string; children: ReactNode }` from `./Manifesto`), `crewCount`(`number`, from `../content/crew-voices`).
- Produces: `export function TecoWay(): JSX.Element` — prop 없음. MDX 전역에 `TecoWay`로 등록되어 `<TecoWay />`로 쓰인다.

- [ ] **Step 1: `components/TecoWay.tsx` 생성**

```tsx
import styles from './TecoWay.module.css'
import { Eyebrow } from './Eyebrow'
import { Claim } from './Manifesto'
import { crewCount } from '../content/crew-voices'

/**
 * TecoWay — 랜딩에서 Hero 다음에 오는 '우테코라는 곳' 선언.
 * 정체성(스스로 자라는 개발자를 기르는 곳) + 성장 방식 3원칙을 Manifesto 스타일로.
 * 바로 뒤 CrewVoiceMap(별자리)의 '함께 자란 동료'가 여기 02를 회수한다.
 * 규모(crewCount)는 content/crew-voices.ts 단일 원천 재사용 → 별자리·행선지와 같은 실.
 */
export function TecoWay() {
  return (
    <section className={styles.section} aria-label="우테코는 어떤 곳이고 크루가 어떻게 자라는가">
      <Eyebrow>우테코라는 곳</Eyebrow>
      <h2 className={styles.heading}>우테코는, 스스로 자라는 개발자를 기르는 곳입니다</h2>
      <p className={styles.lede}>
        2018년부터 10개월 과정을 굴리며, 지금까지 크루 <strong>{crewCount}명</strong>이 이곳을 거쳐 갔습니다.
      </p>

      <div className={styles.claims}>
        <Claim n="01">진짜 미션으로 배웁니다. 강의를 듣는 게 아니라, 손으로 미션을 풀며 익힙니다.</Claim>
        <Claim n="02">페어와 코드리뷰로 함께 자랍니다. 혼자 빨리 가기보다, 동료와 더 멀리 갑니다.</Claim>
        <Claim n="03">스스로 '왜'를 묻습니다. 정답을 받기보다, 자기 근거를 세웁니다.</Claim>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: `components/TecoWay.module.css` 생성**

형제 섹션 `CrewJourney.module.css`의 `.section`·`.heading`·`.lede` 토큰을 그대로 따르고, 원칙 블록만 좌측 정렬 래퍼를 더한다. (`.lede`의 `line-height: 1.6 !important`는 Nextra가 `article p` 줄높이를 2rem로 강제하는 걸 이기기 위한 것 — 반드시 유지.)

```css
/* TecoWay — 랜딩 'Hero 다음' 선언. 헤더는 형제 섹션(CrewJourney)과 동일 토큰, 원칙은 좌측 정렬. */
.section {
  max-width: 52rem;
  margin: 0 auto;
  padding: 5rem 1rem 3.75rem;
  text-align: center;
}

.heading {
  font-size: clamp(1.5rem, 3.6vw, 2.05rem);
  font-weight: 800;
  letter-spacing: -0.03em;
  color: rgb(55, 53, 47);
  margin: 0 0 0.85rem;
  word-break: keep-all;
}

:global(.dark) .heading {
  color: rgba(255, 255, 255, 0.95);
}

.lede {
  font-size: 1.05rem;
  line-height: 1.6 !important;
  color: rgba(55, 53, 47, 0.82);
  max-width: 34rem;
  margin: 0 auto;
  word-break: keep-all;
  text-wrap: pretty;
}

:global(.dark) .lede {
  color: rgba(255, 255, 255, 0.74);
}

.lede strong {
  font-weight: 800;
  color: rgb(55, 53, 47);
}

:global(.dark) .lede strong {
  color: rgba(255, 255, 255, 0.95);
}

/* 원칙 3개 — 가운데 정렬 헤더 아래, 읽기 폭으로 좁혀 좌측 정렬 */
.claims {
  max-width: 34rem;
  margin: 2.25rem auto 0;
  text-align: left;
}
```

- [ ] **Step 3: `mdx-components.tsx`에 등록**

import 블록에서 `import { CrewJourney } from './components/CrewJourney'`(현재 10번째 줄) 바로 다음에 추가:

```tsx
import { TecoWay } from './components/TecoWay'
```

그리고 `useMDXComponents`의 return 객체에서 `CrewJourney,` 다음에 추가:

```tsx
    CrewJourney,
    TecoWay,
```

- [ ] **Step 4: `components/index.ts`에 export 추가**

`export { CrewJourney } from './CrewJourney'` 다음 줄에 추가:

```ts
export { TecoWay } from './TecoWay'
```

- [ ] **Step 5: `content/index.mdx`에 삽입**

Hero 블록(`<Hero ... />`) 바로 다음, 기존 `<Reveal><CrewJourney /></Reveal>` **앞**에 한 줄 삽입. 이 태스크에서는 나머지 순서를 바꾸지 않는다.

```mdx
<Hero
  eyebrow="우아한테크코스 공식문서"
  title="소프트웨어 생태계에 선한 영향력을"
  underline="선한 영향력"
  description="우리가 한 교육 실험과 경험들이 우테코를 넘어 누군가의 성장과 변화를 돕는 데 쓰이길 바랍니다."
  cta={{ label: '우테코 교육 들여다보기 →', href: '/education' }}
/>

<Reveal><TecoWay /></Reveal>

<Reveal><CrewJourney /></Reveal>

<Reveal><CrewVoiceMap bleed /></Reveal>

<Reveal><EnterDocs /></Reveal>

<Reveal><BookNote /></Reveal>
```

- [ ] **Step 6: dev 서버로 컴파일·렌더 확인**

dev 서버가 이미 떠 있는지 확인하고, 없으면 백그라운드로 실행:

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 || echo "not running"
# not running 이면: npm run dev  (백그라운드)
```

브라우저에서 `http://localhost:3000` 을 연다. Playwright MCP가 있으면:
- `browser_navigate` → `http://localhost:3000`
- `browser_take_screenshot` (데스크톱 폭)
- `browser_resize` 390×800 후 다시 스크린샷 (모바일)

확인 항목(육안):
1. Hero 바로 아래 TecoWay가 뜨고, eyebrow "우테코라는 곳" + 제목 + 735 리드 + 원칙 01/02/03이 보인다.
2. 원칙 번호(01/02/03)와 문장이 좌측 정렬로 읽기 폭 안에 있다.
3. Next 컴파일 에러 오버레이(빨간 화면)가 없다.
4. 다크모드 토글 시 제목·본문 대비가 충분하다(형제 섹션과 동일 톤).

Expected: 위 4개 모두 충족. 실패 시 해당 Step으로 돌아가 수정.

- [ ] **Step 7: 카피 규칙 자가 점검**

`TecoWay.tsx` 카피에 em-dash(`—`)가 없고 모든 서술이 합니다체인지 눈으로 확인(린터가 이 파일을 스캔하지 않으므로 수동). 참고로 다음은 0을 출력해야 한다:

```bash
grep -c "—" components/TecoWay.tsx
```

Expected: `0`

- [ ] **Step 8: 커밋 (정확한 경로만)**

```bash
git add components/TecoWay.tsx components/TecoWay.module.css mdx-components.tsx components/index.ts content/index.mdx
git commit -m "feat(landing): TecoWay 섹션 추가 — Hero 다음 '우테코라는 곳' 선언

우테코 정체성 + 성장 3원칙(미션·페어/리뷰·'왜')을 Manifesto 스타일로.
crewCount 재사용으로 735 throughline 시작. 별자리 02 회수 setup.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

> 커밋 전 `git status --short`로 위 5개 파일만 staged인지 확인. `CrewGrowth`·`journey.mdx` 등이 섞였으면 `git restore --staged <그 파일>`로 제외.

---

### Task 2: 별자리를 행선지 앞으로 이동 + 상호참조 카피 정정

최종 서사 순서(Hero → TecoWay → 별자리 → 행선지 → 초대)로 재배열하고, 순서가 바뀌며 어긋나는 지시 카피를 다시 꿴다. 이 태스크만으로 "735 throughline이 세 섹션에 걸쳐 이어지는가"를 독립적으로 리뷰할 수 있다.

**Files:**
- Modify: `content/index.mdx` (`CrewVoiceMap`을 `CrewJourney` **앞**으로)
- Modify: `components/CrewJourney.tsx:50-54` (lede를 throughline으로)
- Modify: `components/EnterDocs.tsx:5-9` (주석의 순서 서술 정정 — 기능 무관)

**Interfaces:**
- Consumes: Task 1의 `TecoWay`(735를 먼저 소개). `crewCount`(`content/crew-voices.ts`).
- Produces: 없음(랜딩 문서 최종 순서 확정). `CrewVoiceMap.tsx:114`의 "앞서 본 {crewCount}명"은 이제 TecoWay가 소개한 735를 가리키므로 **수정 없이 유효**하다.

- [ ] **Step 1: `content/index.mdx` 순서 확정**

Task 1 이후 상태(`TecoWay → CrewJourney → CrewVoiceMap`)에서 `CrewVoiceMap`과 `CrewJourney`의 순서만 맞바꾼다. Hero/EnterDocs/BookNote는 그대로. 최종:

```mdx
<Reveal><TecoWay /></Reveal>

<Reveal><CrewVoiceMap bleed /></Reveal>

<Reveal><CrewJourney /></Reveal>

<Reveal><EnterDocs /></Reveal>

<Reveal><BookNote /></Reveal>
```

- [ ] **Step 2: `components/CrewJourney.tsx` lede를 throughline으로**

50~54번째 줄의 `<p className={styles.lede}>` 블록을 아래로 교체한다. 별자리가 바로 앞에 오므로 그 흔적을 이어받는다.

교체 전:
```tsx
      <p className={styles.lede}>
        이 문서에 담긴 경험과 문화로 자란 크루 <strong>{crewCount}명</strong>이,
        <br />
        지금 이런 회사들에서 IT 생태계에 영향력을 펼치고 있습니다.
      </p>
```

교체 후:
```tsx
      <p className={styles.lede}>
        앞서 별자리를 남긴 그 크루 <strong>{crewCount}명</strong>이,
        <br />
        지금 이런 회사들에서 IT 생태계에 영향력을 펼치고 있습니다.
      </p>
```

- [ ] **Step 3: `components/EnterDocs.tsx` 주석 정정**

5~9번째 줄 블록 주석의 순서 서술만 새 순서로 고친다(렌더 무관, 정확성 유지).

교체 전(해당 줄):
```tsx
 * 열망(Hero) → 증거(CrewJourney·CrewVoices)를 본 독자에게, 독자별 입구를 준다.
```

교체 후:
```tsx
 * 열망(Hero) → 우테코란 곳(TecoWay) → 흔적(CrewVoiceMap) → 확산(CrewJourney)을 본 독자에게, 독자별 입구를 준다.
```

- [ ] **Step 4: dev 서버로 순서·throughline 육안 확인**

`http://localhost:3000` 새로고침(dev 서버 HMR). Playwright MCP가 있으면 `browser_navigate` 후 전체 페이지 스크린샷.

확인 항목:
1. 순서가 Hero → TecoWay → 별자리(CrewVoiceMap, 풀블리드 밤하늘) → 행선지(CrewJourney, 궤도) → 초대(EnterDocs) 로 흐른다.
2. **735 throughline 3곳이 이어진다:** TecoWay 리드 "크루 735명이 이곳을 거쳐" → 별자리 서브 "앞서 본 735명이 우테코를 수료하며" → 행선지 lede "앞서 별자리를 남긴 그 크루 735명이".
3. 별자리 풀블리드(`bleed`)가 순서 이동 후에도 정상(좌우 여백 쏠림 없음), 모바일 폭(≤680px)에서 행선지 궤도가 칩 목록으로 폴백된다.
4. 컴파일 에러 없음.

Expected: 4개 모두 충족.

- [ ] **Step 5: 타입/빌드 게이트**

`next build`로 타입·컴파일 안전성을 최종 확인한다(`npm run build`는 postbuild로 llms-txt·pagefind까지 도는 무거운 훅이라, 타입 게이트만 필요하면 `next build` 직접 호출):

```bash
npx next build
```

Expected: "Compiled successfully" 및 라우트 목록 출력, 타입 에러 0.
(참고: `next dev`가 같은 `.next`를 쓰고 있으면 잠깐 충돌할 수 있다. 에러 시 dev 서버를 잠시 멈추고 재시도.)

- [ ] **Step 6: 커밋 (정확한 경로만)**

```bash
git add content/index.mdx components/CrewJourney.tsx components/EnterDocs.tsx
git commit -m "feat(landing): 별자리를 행선지 앞으로 이동 + 735 throughline 정렬

서사 순서를 Hero→TecoWay→별자리→행선지→초대로 확정. CrewJourney lede가
바로 앞 별자리를 이어받고, 735가 TecoWay→별자리→행선지로 한 실처럼 꿰인다.
EnterDocs 주석도 새 순서로 정정.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

> 커밋 전 `git status --short`로 위 3개 파일만 staged인지 확인.

---

## Self-Review

**1. Spec coverage** (스펙 각 항목 → 태스크 대응):
- 신규 `TecoWay.tsx`+`.module.css` → Task 1 Step 1–2 ✓
- Manifesto/`Claim` 재사용, `crewCount` 단일 원천 → Task 1 Step 1 ✓
- 초안 카피(제목·리드·3원칙) → Task 1 Step 1 (em-dash 제거해 두 문장 합니다체로 확정) ✓
- 배선(mdx-components, index.ts) → Task 1 Step 3–4 ✓
- index.mdx 재배열(별자리↑) → Task 2 Step 1 ✓
- 상호참조 카피: CrewVoiceMap "앞서 본" 유지(무수정) → Task 2 Step 2 Interfaces 명시 ✓ / CrewJourney lede throughline → Task 2 Step 2 ✓ / EnterDocs 주석 → Task 2 Step 3 ✓
- updates.ts·_meta.ts 해당 없음 → 태스크 없음(스펙과 일치) ✓
- 검증(dev 육안·lint·build) → Task 1 Step 6–7, Task 2 Step 4–5 ✓
- 범위 밖(CrewGrowth/journey 불간섭, 커밋 격리) → Global Constraints ✓

**2. Placeholder scan:** TBD/TODO/"적절히 처리" 없음. 모든 코드 스텝에 실제 코드·명령·기대 출력 포함. ✓

**3. Type consistency:** `TecoWay`(무 prop) — Task 1 정의, Task 2에서 `<TecoWay />`로만 참조. `crewCount: number`·`Claim({n,children})`·`Eyebrow({children})` 시그니처는 실제 파일에서 확인한 그대로. `CrewVoiceMap`의 `bleed` prop 유지. ✓

갭 없음.

## Execution Handoff

계획을 `docs/superpowers/plans/2026-07-01-랜딩-서사-아크.md`에 저장했다. 실행 방식 두 가지:

1. **Subagent-Driven (권장)** — 태스크마다 새 서브에이전트 dispatch, 태스크 사이 리뷰, 빠른 반복.
2. **Inline Execution** — 이 세션에서 executing-plans로 배치 실행 + 체크포인트 리뷰.
