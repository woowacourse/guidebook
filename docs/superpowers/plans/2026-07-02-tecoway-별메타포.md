# TecoWay 별 메타포 재구성 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 랜딩 `components/TecoWay.tsx`의 뻔한 3원칙(`Claim`)을, 빛나는 행성이 캐릭터 + 별 메타포 서사 한 단락 + `/education/journey` 링크로 재구성한다.

**Architecture:** 단일 컴포넌트 재구성. `Claim` import/사용을 제거하고 `Eyebrow`·`crewCount`는 유지. 성장 여정 페이지(`/education/journey`)의 행성이 캐릭터를 랜딩에도 등장시켜 두 페이지가 한 우주를 공유한다. CSS는 형제 섹션 `CrewJourney`의 glow·라인아트 다크 반전 패턴을 재사용한다. 6단계 성장 레일은 복제하지 않고 링크로만 연결한다.

**Tech Stack:** Next.js 15 App Router, Nextra 4, React 19, CSS Modules, TypeScript 5.7. 컴포넌트 테스트 프레임워크 없음 — 검증은 라이브 dev 서버 SSR + 정적 `lint:mobile` + em-dash grep.

## Global Constraints

- **카피 규칙:** 사용자 노출 본문은 합니다체(~합니다/~습니다/~입니다). em-dash(`—`, U+2014) 금지 — 쉼표·마침표로 대체. 신규 코드 주석에도 em-dash를 쓰지 않아 `grep -c "—"` 전체 파일 = 0을 만든다(결정적 검증).
- **단일 원천:** 크루 수는 `crewCount`(`content/crew-voices.ts`)를 import해 쓴다. 735를 하드코딩하지 않는다.
- **735 throughline 보존:** 새 서사가 "…크루가 지금까지 {crewCount}명입니다"로 735를 소개해야, 다운스트림 `CrewVoiceMap`("앞서 본 735명")·`CrewJourney`("그 735명")의 지시 대상이 유지된다. 735 언급을 없애지 않는다.
- **캐릭터:** 기존 18종에서만 고른다 — 이 작업은 `행성이-반짝`(빛나는 별)을 쓴다. 신규 일러스트 없음.
- **모바일 안전:** 캐릭터 `.pose`는 `max-width: 100%`. 장식 glow는 bare-px 고정폭 금지(정적 린트 `fixed-width-no-max` error 회피) — `width: min(240px, 60vw)` 사용. `lint:mobile`에서 `TecoWay.module.css` 신규 findings 0건.
- **Nextra 링크 밑줄:** journey 링크는 Nextra의 `article a:hover` 밑줄 강제를 끄기 위해 링크 클래스에 `text-decoration: none !important`(기존 Card/AssetCard/EnterDocs 패턴).
- **커밋 격리:** Stop 훅이 `git add -A`로 쓸어 담고 병렬 세션 WIP가 main에 올라온다. 정확히 2개 파일(`components/TecoWay.tsx`, `components/TecoWay.module.css`)만 `git add`. `git add -A`/`.` 금지. 커밋 직전 `git status --short`로 확인, 섞이면 `git restore --staged <파일>`로 제외.
- **불변:** `CrewVoiceMap`·`CrewJourney`·`content/growth-arc.ts`·`/education/journey`·`mdx-components.tsx`·`components/index.ts`·`_meta.ts`·`updates.ts`는 건드리지 않는다(TecoWay는 이미 등록·export됨).

---

### Task 1: TecoWay를 별 메타포 서사로 재구성

`components/TecoWay.tsx`와 `components/TecoWay.module.css`를 함께 교체한다. tsx(구조·카피)와 css(캐릭터·glow·링크)는 하나의 응집된 deliverable이라 한 태스크로 묶는다.

**Files:**
- Modify: `components/TecoWay.tsx` (전체 교체)
- Modify: `components/TecoWay.module.css` (전체 교체)

**Interfaces:**
- Consumes: `Eyebrow`(`{ children }` from `./Eyebrow`), `crewCount`(`number`, from `../content/crew-voices`), `Link`(from `next/link`).
- Produces: `export function TecoWay(): JSX.Element` — prop 없음. MDX 전역 등록·index.ts export는 이미 존재하므로 변경 불필요. `Claim`은 더 이상 쓰지 않는다(philosophy.mdx 등 다른 사용처는 그대로).

- [ ] **Step 1: `components/TecoWay.tsx` 전체 교체**

아래 내용으로 파일 전체를 덮어쓴다. (기존 `Claim` import·3원칙 제거, `next/link`·캐릭터·서사·journey 링크 추가. 서사·주석에 em-dash 없음.)

```tsx
import Link from 'next/link'
import styles from './TecoWay.module.css'
import { Eyebrow } from './Eyebrow'
import { crewCount } from '../content/crew-voices'

/**
 * TecoWay: 랜딩에서 Hero 다음에 오는 '우테코의 10개월' 선언.
 * 작은 행성이가 스스로 빛나는 별이 되어 생태계로 걸어 나가는 별 메타포 서사로,
 * 미션·페어·스스로 '왜'라는 우테코의 how를 이야기에 녹인다.
 * 바로 뒤 CrewVoiceMap(별자리) '함께 자란 동료'와 CrewJourney(행선지)로 이어지는 setup이자,
 * 735 throughline의 시작점(crewCount는 content/crew-voices.ts 단일 원천).
 * 성장 여정 전체(레벨 0~5)는 /education/journey(CrewGrowth)로 연결한다.
 */
export function TecoWay() {
  return (
    <section className={styles.section} aria-label="우테코의 10개월, 작은 행성이가 빛나는 별이 되기까지">
      <Eyebrow>우테코의 10개월</Eyebrow>

      <span className={styles.figure} aria-hidden="true">
        <span className={styles.glow} />
        <img className={styles.pose} src="/images/characters/행성이-반짝.png" alt="" width={112} />
      </span>

      <h2 className={styles.heading}>작은 행성이가, 스스로 빛나는 별이 되어 떠납니다</h2>
      <p className={styles.lede}>
        우테코의 10개월은, 작은 행성이 하나가 낯선 별에 내려앉는 데서 시작합니다. 강의를 듣는 대신 진짜
        미션을 손으로 풀고, 혼자가 아니라 페어와 코드리뷰로 동료와 함께 부딪히고, 답을 받기보다 스스로
        '왜'를 물으며 한 단계씩 밝아집니다. 레벨 0에서 5를 지나는 사이 스스로 빛나게 된 크루가 지금까지{' '}
        <strong>{crewCount}명</strong>입니다. 이 별들이 이제 소프트웨어 생태계로 걸어 나갑니다.
      </p>

      <Link className={styles.journeyLink} href="/education/journey">
        10개월, 어떻게 자라는지 →
      </Link>
    </section>
  )
}
```

- [ ] **Step 2: `components/TecoWay.module.css` 전체 교체**

아래 내용으로 파일 전체를 덮어쓴다. `.heading`·`.lede`(+다크) 토큰은 유지, `.claims` 제거, 캐릭터(`.figure`/`.glow`/`.pose`)·journey 링크 추가. glow는 `min(240px, 60vw)`로 모바일 안전. 주석에 em-dash 없음.

```css
/* TecoWay: 랜딩 'Hero 다음' 선언. 헤더 토큰은 형제 섹션(CrewJourney)과 동일.
   작은 행성이(반짝) + 은은한 glow로 '스스로 빛나는 별' 메타포를 연다. */
.section {
  max-width: 52rem;
  margin: 0 auto;
  padding: 5rem 1rem 3.75rem;
  text-align: center;
}

/* 캐릭터 + glow (장식) */
.figure {
  position: relative;
  display: inline-flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 1.1rem;
}

.glow {
  position: absolute;
  left: 50%;
  top: 50%;
  width: min(240px, 60vw);
  height: min(240px, 60vw);
  transform: translate(-50%, -50%);
  z-index: -1;
  pointer-events: none;
  background: radial-gradient(
    50% 50% at 50% 50%,
    rgba(255, 219, 170, 0.5),
    rgba(255, 219, 170, 0) 70%
  );
}

:global(.dark) .glow {
  background: radial-gradient(
    50% 50% at 50% 50%,
    rgba(255, 230, 195, 0.16),
    rgba(255, 230, 195, 0) 70%
  );
}

.pose {
  height: auto;
  max-width: 100%;
  mix-blend-mode: multiply; /* 라이트: 흰 배경이 종이에 녹는다 */
}

:global(.dark) .pose {
  filter: invert(1); /* 다크: 검은 선을 흰 선으로 */
  mix-blend-mode: screen;
}

.heading {
  font-size: clamp(1.5rem, 3.6vw, 2.05rem);
  font-weight: 800;
  letter-spacing: -0.03em;
  color: rgb(55, 53, 47);
  margin: 0 0 0.85rem;
  word-break: keep-all;
}

:global(.dark) .heading {
  color: rgba(255, 255, 255, 0.95);
}

.lede {
  font-size: 1.05rem;
  line-height: 1.6 !important;
  color: rgba(55, 53, 47, 0.82);
  max-width: 34rem;
  margin: 0 auto;
  word-break: keep-all;
  text-wrap: pretty;
}

:global(.dark) .lede {
  color: rgba(255, 255, 255, 0.74);
}

.lede strong {
  font-weight: 800;
  color: rgb(55, 53, 47);
}

:global(.dark) .lede strong {
  color: rgba(255, 255, 255, 0.95);
}

/* journey 링크: Nextra의 article a:hover 밑줄 강제를 끈다(Card/AssetCard/EnterDocs 패턴) */
.journeyLink {
  display: inline-block;
  margin-top: 1.6rem;
  font-size: 0.95rem;
  font-weight: 700;
  color: rgb(55, 53, 47);
  text-decoration: none !important;
}

.journeyLink:hover {
  opacity: 0.72;
}

:global(.dark) .journeyLink {
  color: rgba(255, 255, 255, 0.92);
}
```

- [ ] **Step 3: 라이브 dev 서버 SSR 검증**

dev 서버는 이미 http://localhost:3000 에서 실행 중이다(새로 띄우거나 멈추지 않는다). HMR 반영을 잠깐 기다린 뒤, 아래를 실행한다.

새 콘텐츠가 렌더되고 옛 3원칙 잔재가 사라졌는지:
```bash
html="$(curl -s http://localhost:3000)"
for s in "우테코의 10개월" "스스로 빛나는 별이 되어 떠납니다" "낯선 별에 내려앉는" "735" "10개월, 어떻게 자라는지" "/education/journey"; do
  printf '%s' "$html" | grep -qF "$s" && echo "✓ $s" || echo "✗ MISSING: $s"
done
# 옛 카피 잔재가 없어야 함(각각 0이어야 정상):
echo "old-heading: $(printf '%s' "$html" | grep -cF "스스로 자라는 개발자를 기르는 곳")"
echo "old-claim01: $(printf '%s' "$html" | grep -cF "진짜 미션으로 배웁니다")"
```
Expected: `✓` 6개 모두, `old-heading: 0`, `old-claim01: 0`. (Next 에러 페이지가 아니라 실제 HTML이 와야 함 = 컴파일 성공.)

- [ ] **Step 4: em-dash + 정적 모바일 린트 검증**

```bash
grep -c "—" components/TecoWay.tsx components/TecoWay.module.css
npm run lint:mobile 2>&1 | grep -i "TecoWay" || echo "TecoWay: 모바일 린트 findings 없음"
```
Expected: 두 파일 모두 em-dash `0`. `TecoWay: 모바일 린트 findings 없음`(TecoWay.module.css에서 새 오버플로우/고정폭 경고 없음). 참고: `Hero.module.css .starGlow` error는 기존 것이며 이 작업과 무관.

- [ ] **Step 5: `npx next build`는 실행하지 않는다**

사용자의 dev 서버가 라이브이고 `.next`를 공유하므로 `next build`는 그 세션을 방해한다. 이 태스크는 신규 타입·파일이 없는 순수 컴포넌트 재구성이라 빌드-타입 리스크가 실질 0이다. Step 3의 SSR 성공이 컴파일 성공을 증명한다.

- [ ] **Step 6: 커밋 (정확히 2개 파일)**

```bash
git add components/TecoWay.tsx components/TecoWay.module.css
git status --short   # 위 2개만 staged인지 확인. 다른 파일 섞이면 git restore --staged <파일>
git commit -m "feat(landing): TecoWay를 별 메타포 서사로 재구성

뻔한 3원칙을 제거하고 빛나는 행성이(반짝) + '작은 행성이가 빛나는 별이 되어
생태계로' 서사 한 단락으로 교체. 미션·페어·스스로 '왜'를 이야기에 녹이고
735 throughline 보존. 랜딩에서 /education/journey 로 첫 링크 신설.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage** (스펙 목표 → 태스크 대응):
- 뻔함 제거(Claim 3개 삭제) → Step 1 ✓
- 10개월을 별 메타포로 자연스레 → Step 1 서사 ✓
- 캐릭터 활용(행성이-반짝) → Step 1 `<img>` + Step 2 `.figure/.glow/.pose` ✓
- 구체성 보존(미션·페어·동료·'왜' 이야기에 녹임) → Step 1 서사 문장 ✓
- 735 throughline 보존 → Global Constraints + Step 1 `{crewCount}명` + Step 3 "735" 확인 ✓
- 랜딩→journey 링크 신설 → Step 1 `<Link href="/education/journey">` + Step 3 확인 ✓
- 다크·모바일·밑줄 회피 → Step 2 `:global(.dark)`·`min(240px,60vw)`·`text-decoration:none !important` + Step 4 린트 ✓
- 배선/데이터 불변 → Global Constraints(변경 파일 2개로 한정) ✓

**2. Placeholder scan:** TBD/TODO/"적절히" 없음. 모든 코드 스텝에 전체 코드·명령·기대 출력 포함. 카피는 스펙에서 승인된 초안 그대로. ✓

**3. Type consistency:** `TecoWay`(무 prop) 시그니처 불변 → 배선 변경 없음. `crewCount: number`, `Eyebrow({children})`, `Link`(next/link) 모두 실제 존재 확인. `.claims` 제거는 tsx·css 양쪽에서 동시(잔여 참조 없음). ✓

갭 없음.

## Execution Handoff

계획을 `docs/superpowers/plans/2026-07-02-tecoway-별메타포.md`에 저장했다. 실행 방식 두 가지:

1. **Subagent-Driven (권장)** — 태스크마다 새 서브에이전트 dispatch, 리뷰, 빠른 반복.
2. **Inline Execution** — 이 세션에서 executing-plans로 실행 + 체크포인트 리뷰.
