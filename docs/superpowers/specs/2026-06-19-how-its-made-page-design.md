# 설계: "이 문서는 어떻게 만들어지는가" 신뢰 페이지

- **날짜:** 2026-06-19
- **작성:** brainstorming 세션 (im1@woowahan.com)
- **상태:** 설계 확정, 구현 진행 중

## 1. 목적과 배경

외부 독자는 이 문서의 **다듬어진 결과물**(철학·커리큘럼·검증된 패턴)만 봅니다. 그래서 "이건 그냥 멋진 말 아닌가? 위에서 내려온 선언이거나 AI가 뽑은 글 아닌가?"라는 의심이 *읽기 전에* 생깁니다. 이 문서의 콘텐츠는 실제로는 **아래에서 위로 승격**됩니다 — 실험 로그에서 귀납되고, 루브릭 점수와 게이트를 통과해야 올라가며, 새 로그가 쌓이면 언제든 수정됩니다.

이 페이지는 그 **제작 과정을 시각적으로 드러내** 신뢰도를 높이는 것이 목적입니다.

### 신뢰의 세 기둥
1. **귀납** — 주장은 위에서 떨어진 게 아니라 실험 로그에서 귀납된다.
2. **게이트** — 감이 아니라 루브릭 점수(품질 25점 / 승격 20점)와 임계값으로 승격된다.
3. **수정 가능** — 박제된 교리가 아니라 언제든 갱신되는 가설이다.

## 2. 위치 · 진입점 (확정)

- **라우트:** `/how-its-made` — 독립 최상위 페이지 (`content/how-its-made.mdx`)
- **상단 nav:** `type: 'page'`, 라벨 **"문서가 만들어지는 법"** (H1은 "이 문서는 어떻게 만들어지는가")
- **홈 진입:** `content/index.mdx` Hero 아래 진입 카드 1개 (홈은 미니멀 유지)
- **철학 교차링크:** `content/education/philosophy.mdx` 마무리 Callout에 "→ 어떻게 검증되는지 보기"
- **청중:** 외부 독자(회의적). **본문은 합니다체**(프로젝트 규약).

### 기존 페이지와의 차별화
`content/education/tools/research-cycle-workflow.mdx`는 **코치가 기계를 돌리는 법**(슬래시 커맨드·dry-run, 내부용)입니다. 이 페이지는 **독자가 결과물을 믿는 법** — 슬래시 커맨드 없이 게이트·점수·숫자·수정가능성에만 집중하고, 둘은 서로 교차링크합니다.

## 3. 페이지 섹션 구조 (파이프라인 조망형)

| # | 섹션 | 핵심 메시지 | 구현 |
|---|------|------------|------|
| 0 | 도입 | "여러분은 다듬어진 결과물만 봅니다. 그게 어떻게 만들어졌는지 보여드립니다." | H1 + 리드 + Callout(테제) |
| 1 | 승격 파이프라인(조망) | 로그 → 게이트 → 도구/패턴 → 게이트 → 커리큘럼/철학 | 비스포크 `PromotionPipeline` |
| 2 | 게이트 들여다보기 | 게이트는 감이 아니라 실제 루브릭 점수다 | 컴포넌트 게이트 클릭 → 점수카드 |
| 3 | 숫자로 받친다 | 주장은 수사가 아니라 측정이다 | 기존 `CardGrid` |
| 4 | 한 주장 끝까지 추적 | "협업은 구조다" ← 51% 질문 ← 원본 로그 ← 게이트 | Mermaid + 산문 |
| 5 | 살아있는 문서 | 박제된 교리가 아님 | 기존 `<RecentUpdates />` |
| 6 | 고정된 잣대 | 루브릭은 사람만 수정 | `Callout` |
| 7 | 더 깊이 | 연구 사이클·인사이트·로그 링크 | `Card` |

## 4. 비스포크 컴포넌트 `PromotionPipeline`

repo 관례(TS + `.module.css` + `:global(.dark)`)로 작성, Folio의 autoresearch 점수카드 패턴 이식. 게이트 클릭 시 해당 루브릭 점수카드(차원 행)를 아코디언으로 펼침. **실제 `.claude/*-rubric.md`와 1:1.** 게이트 점수카드의 막대는 *특정 로그의 점수가 아니라 루브릭 기준(통과선·차원 정의)*을 보여줌 — 가짜 점수 비노출.

- 품질 루브릭 5차원(D1 구조완성도·D2 구체성·D3 전이가능성·D4 교훈양면성·D5 원본연결), 25점, 통과 16점(B).
- 승격 루브릭 4차원(P1 반복검증·P2 추출가능성·P3 교차연결·P4 실행영향력), 20점, 통과 16점.
- 액센트 `#4285f4`, Notion 팔레트 표면, 다크모드, `prefers-reduced-motion` 존중, 모바일 세로 전환.

## 5. 정량 근거 (출처 고정)
`content/education/logs/react-payments-555prs-analysis.mdx`에서 인용, 출처 명시: 5년/555 PR · 215명 중 97% 재방문 · 코멘트 2,178건 중 51% 질문.

## 6. 한 주장 추적
Mermaid: 철학(협업은 구조다) ← 데이터(51% 질문) ← 원본(react-payments 로그) ← 게이트(루브릭 통과).

## 7. 파일 변경 요약
**신규:** `content/how-its-made.mdx`, `components/PromotionPipeline.tsx`, `components/PromotionPipeline.module.css`
**수정:** `mdx-components.tsx`, `components/index.ts`, `content/_meta.ts`, `content/index.mdx`, `content/education/philosophy.mdx`, `content/updates.ts`

## 8. 범위 밖 (YAGNI)
- 풀 스크롤리텔링/애니메이션 (핵심 1개 비스포크만)
- Folio JSX 직접 import (전역 `window.React` 의존 → 패턴만 차용)
- 개별 로그의 실제 채점 점수 노출 (기준 자체를 보여주는 게 목적)
- 별도 stat-tile 컴포넌트 (기존 CardGrid로 충분)

## 9. 검증 기준
- `npm run build` 통과, `/how-its-made` 렌더
- `npm run lint:tone` 통과 (합니다체)
- 3개 진입점 도달 가능
- 루브릭 차원·점수가 `.claude/*-rubric.md`와 일치
- 다크모드·모바일 폭에서 안 깨짐
